# v0.6.0 — tag readiness

**Rebuilt 2026-09-26 for Robert's definition of done.** Written against `main`
at `f3926a1` (the C6/C26 token path included). **D3, D10, D13, Q1, Q6, W3, W5
and §4 re-checked against `main` after `dad278b`, with the credential-grant
narrowing on top** (boundary review of `8a61989`). **D5, D6, D12 and §4
corrected after an independent audit at `ee350c6`**, which found all 18 cited
SHAs on `main`. Rows marked
"reported" come from the Linux session's findings as relayed on 2026-09-26,
and were not re-measured by the author of this list.

## 0. 🔴 THE ACCEPTANCE BAR: the question this document answers

**Robert, 2026-09-26, set as the gate for tagging v0.6.0:**

> Two Managers on the same Linux machine — Claude plus a local LLM —
> working without issues, or at least verified enough that it's ready for
> dogfood testing.

**Why it is a gate and not a sentence:** in v0.5.0, work was declared done
that was not. The row percentages in §1 count rows. If every row were green
and this bar unmet, they would say 100% and be wrong. So
`tools/tag_readiness.py` reports this bar FIRST and separately: MET, NOT
MET or NOT YET OBSERVED. It is MET only when every part below is ✅, and no
arithmetic over §1 can satisfy it.

**"Without issues" made checkable.** Each part is observed on Linux, on one
machine and one project root, with a real Claude Owner and a real local
model. Markers: ✅ met, ❌ not met, ⏳ not yet observed.

| # | observable part | status on Linux | evidence |
|---|---|---|---|
| A1 | Both Managers, a Claude Owner and a local-model secondary, running at once on one Linux machine in one project root | ⏳ | D5's Linux cell is "—". The Linux Claude cycle is in progress (the boundary session). On macOS it has now run with real engines on both sides: a Claude Owner and a Goose/`qwen3-32b` secondary in one concurrent run, with A2–A4 observed there too (D5, 2026-09-26). That is macOS evidence and does not move this Linux cell |
| A2 | The Owner routes to the secondary (`rite route`) | ⏳ | not observed on Linux with real engines |
| A3 | The secondary acts on what was routed | ⏳ | not observed on Linux with real engines |
| A4 | The secondary's reply arrives at a still-running Owner | ⏳ | not observed on Linux with real engines |
| A5 | The inbox fence holds under real concurrent load | ⏳ | the fence is tested at kernel level on Linux (MM-2, `test_landlock_really_confines.py`), not under two Managers running at once |
| A6 | The local half is a model that actually RUNS `rite reply` (a tool call), not one that prints it as text | ❌ | D4: `qwen3:1.7b` on the VM did not run `rite reply` when asked. A larger model (4B/8B) is not yet measured on that CPU. On macOS, `qwen3-32b-ctx32k` ran `rite reply` in both D5 runs but printed it as text in the setup-session run (2026-09-26), so even 32B is not reliable |

**Rough edges are named, not hidden.** The standard is "verified enough
for dogfood", not perfect, so a pass that carries named rough edges IS a
pass. What is not allowed is masking one. Known so far:
- **The Owner cannot wait for its secondary** (W13). OBSERVED on macOS in
  D5's first attempt, which failed for exactly this reason; the second
  passed only because the Owner was told how to poll. To be confirmed on
  Linux in A1–A4's run. It must be stated in the notes as a rough edge.
  A retry must not paper over it.

**The target is Wednesday:** a functional **Claude and local** multi-Manager
setup, **driven through Slack**, **working on macOS and Linux**. Linux is a
requirement, not a deferral. A slip of 1–2 days is acceptable, and shipping
something subpar to hit the date is not.

**Re-check every line against `main` on the day.** This project has shipped
functions that were correct and uncalled.

## ⚠ The headline: every feature observation is macOS

Every row below that says "observed" was observed on **macOS**: the Slack
relay, check-ins, multi-Manager, the credential work, text cleaning, the
local engine and the sandbox.

**⚠ UPDATED 2026-09-26: Linux now has observations** (D1, D2, D4, D7, D8, D9, D16–D18, on a Parallels Ubuntu ARM64 VM at `6ea56f8`, by the Slack and check-ins session). The paragraph below is the state before them. **Linux had no observation of rite working.** The Linux evidence that exists
is:
- the unit suite in CI, which on `main` fails the four tests that start a
  real Manager;
- the Landlock boundary's own probes (spike B9b, and CI on `ubuntu-latest`
  on the backend branch).

**No Manager has completed a cycle on Linux.** A Manager cannot start there
on `main` at all (C28).

Legend:
- **✅ obs** means observed by a person running rite;
- **🧪 test** means tests or probes only;
- **❌** means not working;
- **✋ manual** means measured by hand in one session, with no test pinning it;
- **🟰 accepted** means a divergence the owner DECIDED to accept, with the date and the reason recorded. It is NOT observed working, and the readiness script counts it separately, never as observed;
- **—** means nothing.

## 1. Against the definition of done

| # | part of "done" | macOS | Linux | what closes the Linux column |
|---|---|---|---|---|
| D1 | **A Manager runs at all, inside a boundary** | ✅ obs. B9 (`4ebbbd7`); tmux and signal escapes closed (`9862b59`); re-measured at `afa41e9` | ✅ **obs 2026-09-26, Parallels Ubuntu 24.04 ARM64 VM, `6ea56f8`, Goose on `qwen3:1.7b` (CPU)**: `sandbox: Manager 'small' runs inside a landlock boundary`; the session ran and the model answered in the pane; `ceiling reached: 1 session(s) started`. ⚠ **Only after two directories were created by hand; see D16.** ⚠ One cycle only: a project with no board gets one setup session, so a multi-cycle run waits on the board (Robert's GitHub login on the VM) | a multi-cycle run on a board; D16 fixed and observed on a FRESH machine |
| D2 | **The tmux socket escape is closed** | ✅ obs. Socket directory denied; every Unix socket except the resolver denied (C6/C26 landing) | 🟰 **ACCEPTED DIVERGENCE: Robert's decision, 2026-09-26** ("yes and yes", as relayed: to accepting it, and to the inbox move). **The escape is OPEN on Linux, and stays open by design.** Observed: from inside the real Landlock ruleset, `tmux new-session -d 'touch ~/escaped_tmux'` through a server started outside created the file. **Why it is accepted, so it is not reopened without new facts:** Landlock does not govern `connect(2)`, and its network rules are TCP-only, so the only closures are a mount namespace or seccomp. Bubblewrap (a mount namespace) is blocked by Ubuntu's default AppArmor policy. Docker needs `docker` group membership, which is ROOT-EQUIVALENT on the host, so closing the escape that way costs more security than it buys. Seccomp cannot inspect a `connect` target: it is block-all-unix-sockets or nothing, and selective blocking needs a user-notify supervisor process. **Kept honest:** it stays in the CHANGELOG's Known issues, and `test_landlock_really_confines.py::TestTheTwoEscapes` asserts the escape OPEN, so a kernel that closes it fails loudly | nothing owed: closed as an accepted divergence. Reopen only with a closure that costs less security than it buys |
| D3 | **A Claude Manager, sandboxed** | ✅ **obs 2026-09-26** (`8a61989`, Q1 decided as (i)): a sandboxed Claude Manager signed in with a `setup-token` token from its own `CLAUDE_CONFIG_DIR`, read the project, and its `rite reply` reached `rite replies`. **Re-observed after the narrowing** (grants by exact path, the login write-denied): it signed in and replied. Also observed through `rite start`: a login left by a killed run removed and reported; a second start for a running Manager refused with its login untouched. 🧪 In the profile, overwrite / append / rename-over / remove of the login all refused. The token is redacted from the journal by exact value (obs via `rite journal observe`) | ⏳ **obs 2026-09-26 at `1ba8ea5`, VM, Claude Code 2.1.283 installed:** with no token stored, `rite start lead` REFUSES before launching: `refusing to start Manager 'lead': a Claude Manager runs inside a sandbox, which cannot read your keychain login, so it needs a token of its own. Run claude setup-token, then rite credential set claude_token …`. The refusal is correct; its wording named a keychain, which Linux does not use (reworded after this, platform-neutral). **A signed-in cycle needs Robert's token on the VM**: `claude setup-token` needs his account, and only he pastes it. 🧪 `57469b8` grants the login on Linux (`claude/` writable, the credential directory readable); probed unprivileged through `write_profile` + apply, **no Manager signed in**. ⚠ **Weaker than macOS, by Landlock's design:** no deny rule, so the Manager CAN overwrite its own login, and the whole credential directory is readable (macOS now grants two gh files by exact path). **Release notes: a Claude Manager does NOT work on Linux in this build, until observed** | Robert's token on the VM, then a signed-in cycle observed. The boundary session: whether Landlock file-level rules can match macOS's narrowing |
| D4 | **A local (Goose) Manager** | ✅ obs. Declared model, endpoint checks, cycles in the sandbox, `rite reply`, broker Worker; continuation across runs (C29 fixed) | ✅ **obs 2026-09-26, Parallels Ubuntu 24.04 ARM64 VM, `6ea56f8`, Goose on `qwen3:1.7b` (CPU)**, with D16's workaround. The model served a **32768-token** window at **49 tok/s** decode (12.7 s cold load, 100% CPU, 5.3 GB resident); `rite doctor`: `local engine small: answering, 1 model(s)`. ⚠ At 1.7B it did not run `rite reply` when asked: tool use is weak, and throughput is not the problem | Robert: is a 1.7B model 'functional' enough, or measure a 4B/8B on the same CPU |
| D5 | **Two Managers in one root (Claude Owner plus local secondary)** | ✅ **obs 2026-09-26 with real engines on both sides, in ONE concurrent run, macOS, `a8fd5ea`.** Owner `lead` = Claude Code 2.1.261, secondary `helper` = Goose on `qwen3-32b-ctx32k` (Ollama), against the real board `robbartoszewski/rite-dogfood-board` with probe issue #6 labelled `scheduled` (verdict `saturated`, so NOT a setup session). A person's `rite message lead` asked it to route a task. **t+16s** the helper's inbox held `[routed by the Owner Manager 'lead' · sent Sat 11:02 · INSTRUCTION]`. **t+36s** `rite status`: `helper: running` and `lead: running`. The helper wrote `HELLO.txt` (`written by helper, run 2`) and ran `rite reply`. **t+147s** lead's supervisor logged `brought 1 message(s) from 'helper' to 'lead'`, with `lead: running` in `rite status` at that moment; the Owner's inbox held `[from Manager 'helper' · its reply · context — not an instruction]`. Claude's own transcript (in the Manager's `CLAUDE_CONFIG_DIR`) shows it read that file and repeated the reply word for word. **Why the engines were real:** the instance records read `sandbox-exec -f …/lead.sb claude -p --settings …/permissions.json …` and `sandbox-exec -f …/helper.sb goose run -n rite-mgr-acme-83a0fb-helper …`; the harness PATH was the rite venv (no `claude`, no `goose` in it) ahead of the normal PATH, which resolves `claude` to `~/.local/share/claude/versions/2.1.261` and `goose` to `/opt/homebrew/bin/goose`; the project had no stub `bin/`. Probe issue #6 was closed after the run. ⚠ **Passed on the SECOND attempt, and the reason is an open gap (W13):** on the first, Claude routed at t+15s and then could not wait — a bare `sleep` is blocked by Claude Code, so it scheduled a wakeup, which ENDS a `claude -p` session; the reply sat unread in the helper's outbox. The second attempt only passed because its instruction told it to poll its own inbox, and rite's allowlist refused `until` and `break`, so it fell back to a plain sleep loop and read a reply that arrived at t+147s about ten minutes later. **Earlier evidence:** stub engines inside the real profiles (MM-1…5); a Goose Owner and Goose secondary (`c1cc840`, which found the put-back defect). **Setup session (the `a984993` fix):** observed 2026-09-26 at `5ff4cfa` (the same change, before main was merged in), real Claude Owner and real Goose secondary, NO board. The person's message did not mention setup or how to reply. Claude routed at t+26s (before the fix, the same setup declined, citing "my explicit mandate … (ticket-backend setup only)") and replied unprompted: "Done: routed 'helper' to write HELLO.txt … I'll relay what it says" and "Separately, on the setup task: .rite/config.yaml currently has ticket_backend.type: none … Which one do you want". The secondary, on the new "setup is the Owner's job" prompt, wrote `HELLO.txt` and left `config.yaml` untouched; it then printed its `rite reply` command as text instead of running it (a local-model behaviour, read from Goose's session store), so that run did not close the round trip | — | a real Claude Owner with a Goose secondary on Linux, after D3 |
| D6 | **Separation between the two** (§5.4.8) | ✅ obs **and now pinned by a test between two Managers**, which §5.4.8 previously recorded as missing: `test_the_manager_profile_denies_what_it_should.py::TestP2BetweenTwoManagersSharingARoot` — six attacks from alpha's profile at beta's (TERM, KILL, a broad `pkill -f`, and tmux send-keys / kill-session / kill-server), plus the control that alpha can still signal its own child. Mutation-tested: a blanket `(allow signal)` fails the three signal tests, an empty `_socket_denials()` fails the three tmux tests. **P1 holds for the per-Manager directories and every inbox** (MM-2, §5.4.8); ❌ P3 and P4 do not, and P1 does not for flat per-project state | ✅ **the signal half, observed 2026-09-26**: `test_landlock_really_confines.py::TestP2BetweenTwoManagersSharingARoot`, with the victim inside Manager B's OWN Landlock domain rather than unconfined, and the same own-child control. Run in a container on kernel 6.12.68-linuxkit at **Landlock ABI 6**; mutation-tested by dropping `SCOPE_SIGNAL` while keeping another scope, which makes A kill B. ⚠ **The tmux half is NOT tested here because it is OPEN on Linux** (D2) — Landlock does not govern `connect(2)`. ⚠ Not run on the Parallels VM; CI's `ubuntu-latest` has ABI 6 (9 Landlock probes, 0 skips), so it runs there on every push | the tmux half follows D2/W2; P3 and P4 are unchanged on both |
| D7 | **Driven through Slack**: DM instructions, broadcast as context, threads, posts, redaction, delivery while stopped | ✅ obs live, including a second person (A3–A6). Only the Owner opens a relay (MM-1, `24bc75c`). **Rate, measured against the real workspace 2026-09-26 at `a984993`** (token verified first: the only `slack_bot_token` in the keychain is under `rite-30ba7cfe`, non-empty, `auth.test` ok for workspace "Rite"; VM relay off; stub engines, since only the Owner polls; every call logged with status and `Retry-After`): one project with two Managers makes **26 `conversations.history` and 4 `conversations.replies` a minute**, inside the 50 floor. More relays on the same app: 2 → 52/min and 3 → 79/min, **with no 429 at either**; 4 → 104/min, with the first 429 about 164 s in. Held at ~130/min (5 relays), the 429s per minute rose 12 → 49 → 55 → 58 → 68 → 71 while the call rate stayed flat, every one carrying `Retry-After: 10`, which the relay ignores (W14). **No message was lost:** across 361 throttled calls, all 9 relays in the two runs delivered both of Robert's real DMs (their cursors rewound to before them), with headers intact; the longest gap without a successful read reached 22.3 s; posts and thread reads were never refused; the 429 was said once per run. **Not observed:** a message whose FIRST read is a 429 — the rewound relay started into the throttled period got 200s first. That a 429 leaves the cursor in place is from the code (`_read_history` only advances `since` on success) | ✅ **obs 2026-09-26, Parallels Ubuntu 24.04 ARM64 VM, `6ea56f8`, Goose on `qwen3:1.7b` (CPU)**. **Posting:** `slack: posted … → D0C4ALD46TF ts 1790391694.066499`, fetched back from Slack as `*small*: Linux relay test (automated): posted from the Parallels VM`. The reply was written from the VM terminal with `rite reply`, not by the model. **Reading:** Robert's real DM (cursor rewound, not retyped) arrived as `1 message(s) sent in the Owner's DM while 'small' was not running`, then `delivering 1 message(s)`. `prompt.txt` shows the header `[Owner's DM · sent Sat 01:18 · rite normalised … · addressed · INSTRUCTION]`, the tag text decoded and `<!-- … -->` unescaped. The token went via stdin into the environment, and rite said so on stderr as designed. One transient `URLError … Temporary failure in name resolution`, reported once, and the relay carried on | threads and a second person on Linux (not repeated; the code is platform-independent) |
| D8 | **Check-ins** (K1–K6) | ✅ obs: K1, K4, K6; K2/K3 real-model, INSIDE the sandbox, 3 of 3 twice (2026-09-26 08:00 and at `a8fd5ea`; K2/K3 rows of the v0.6.0 plan). *This cell said "outside the sandbox, because of D3", which went stale when D3 was fixed;* K5 posting live (`68f3060`) | ✅ **obs 2026-09-26, Parallels Ubuntu 24.04 ARM64 VM, `6ea56f8`, Goose on `qwen3:1.7b` (CPU)**: `slack: posted … → D0C4ALD46TF ts 1790391375.727519`, then `slack: mirrored the check-in → C0C4KB709T6 ts 1790391376.206019`; the mirror reads `*small* (check-in, mirrored from the Owner's DM; replies here are read as context): Check-in — small …` | K2/K3 with a real model on Linux |
| D9 | **Ticket and Slack text cleaned; phrases reported** (N1, N2) | ✅ obs, both clauses. **Slack keeps hidden characters, and rite removes them** (`9d09507`), so the cleaning stands on rite's code, not on Slack's client. Slack's HTML entities are unescaped (`ed707f0`) | ✅ **Slack clause obs on Linux** (D7: the same message, normalised and unescaped). ⏳ The board clause waits on the VM's GitHub login | platform-independent code; one Linux board read would observe it |
| D10 | **GitHub credentials** (C6/C26, token only) | ✅ **obs against GitHub 2026-09-26**, App `5084143` / installation `165090155` on `rite-dogfood-board`, from a copy of `~/AI/rite`'s config (the checkout itself untouched): **(1)** a real mint, all four permissions asked, accepted. **(2)** inside the real profile with the pane's env: `/installation/repositories` answered (installation tokens only), `gh auth status` = `x-access-token` from the Manager's own `hosts.yml`, the board is PRIVATE, `~/.config/gh` refused; the Manager's `rite board list --status closed` read all 6 issues, and a real sandboxed Claude did the same ("6 tickets; logged in as x-access-token"). **(3)** refresh rewrote the Manager's `hosts.yml`; a token revoked at GitHub mid-run gave `HTTP 401: Bad credentials` on gh and `rite board list`, even `/rate_limit` (no anonymous fall); the next refresh restored the read. ⚠ **This found a defect, fixed:** `refresh(now=…)` signed its JWT with `now`, so a refresh decided early was stamped in the future and GitHub answered 401; the refresh path had never minted anything real. **(4)** `git push` over HTTPS through gh's helper created and deleted a probe branch (confirmed gone). ⚠ **Only with `commit.gpgsign=false` and `--no-verify`:** the operator's global SSH commit signing (`~/.ssh` unreadable) and global Node pre-push hook both fail inside the sandbox. GitHub's event log did not show the push's actor, so attribution rests on the sandbox: the helper list reset to gh, gh reading only the Manager's `hosts.yml`, `~/.config/gh` and the keychain refused | ⏳ **Verified on macOS, UNVERIFIED on Linux, by Robert's decision (2026-09-26)**, recorded as a considered gap. **Tested on Linux:** the sandbox half. Kernel-level tests in Linux CI (the Landlock probe step, 18 passed on each Python) show that only the Manager's two gh files are readable (`9498f9c`, tests `c76ac90`), and that `~/.config/gh` is no longer granted (`9431ccb`). *(It also said the Claude login cannot be replaced on Linux; `248ba5b` grants `claude/` as a tree there, so on Linux it CAN be.)* **Not done on Linux:** a mint, a board read with the token, a push, expiry. **Reasons recorded:** the App code is the same on both platforms; the platform-specific part (the grant) is tested; verifying would put a second copy of a private key on a machine treated as disposable, a day before the tag. ⚠ **One premise of the decision was wrong and is corrected here:** a Linux Manager does NOT work with a `gh` login. Since `9431ccb` no Manager on either platform reads `~/.config/gh`, so the App is the ONLY GitHub credential a Manager has, on Linux too, and not an optional path. Robert may want to reconsider with that in view. A real Linux Manager reading its own credential file inside Landlock is the boundary session's Claude cycle, which is running now | Robert's call, with the corrected premise; otherwise closed as a recorded gap |
| D11 | **The Manager runs the `rite` that started it** (C27) | ✅ obs: pipx, checkout, uv tool | 🧪 the backend branch hit `PermissionError: …/VERSION` and fixed it; not observed with a Manager. On Linux rite has run inside Landlock only as a kernel-level import (`test_landlock_really_confines.py`, granting `_running_rite()`), never as a Manager running the `rite` CLI | a Linux Manager observed running `rite` (e.g. `rite reply`) from inside its boundary. *This column said "covered by D1's observation", which it is not:* D1's Manager never ran `rite`. D4's 1.7B model did not run `rite reply`, and D7's reply was typed at the VM terminal, outside the boundary. A Linux Claude sign-in (D3) or a larger local model would observe it |
| D12 | **CI green** | ✅ **The macOS job is on `main`** since the boundary branch merged at `2268033` (`ci.yml:142`, `runs-on: macos-latest`), **green on that exact SHA**: run `36225801221`, all four jobs (`boundary-and-credentials-on-macos` and `lint-and-test` on 3.11/3.12/3.13). ⚠ **Scope: it runs SIX NAMED FILES, not the suite** (the profile, inbox, Claude sign-in, GitHub credential, file store and backend-choice tests), because a hosted runner has no gitleaks and no credential store and a full run would skip what it exists to check. **It is not macOS coverage of the suite.** The full suite runs on macOS only locally (4572 passed, 29 skipped at `ee350c6`), which is not CI. Its first runs, on the branch, failed and found 4 defects | ✅ **CI fully green on `main` at `691a248`** (run `36221689241`), the first time in this thread: Python 3.11 4563 passed / 38 skipped, 3.12 and 3.13 4562 / 39, 0 failed. C28 is closed. ✅ **The publish gate is GREEN at `691a248`** (run `36221689224`), CLOSED: it had failed on every push from at least `d122c2a` on 5 gitleaks findings in test fixtures, and another session suppressed all five in `.rite/gitleaksignore`, each with a reason pinned to its content (`6080140`), including this list's author's fake `ghs_` token. ⚠ **Known flaky under load, not a defect:** `test_manager_endings_and_resume.py::test_a_killed_manager_is_a_crash_not_an_ambiguity` failed once and passed 3/3 on rerun (reported by the session that saw it). A single failure of it on tag day is this, until it fails on a rerun | the macOS job merged to `main` and green there |
| D13 | **Credentials stored and found headless** (the Slack bot token, board tokens, the App key) | ✅ obs (`a732682`, Q6 decided): one 0600 file store; `rite doctor` names it; `rite credential import-keychain` copied 13 credentials from the old keychain; a wrong mode is refused. A value taken from the environment is announced | ⏳ the same code, **not observed on Linux**. The VM's Slack run took its token from the environment and said so on stderr, as designed | one `rite credential set` + `rite doctor` on the VM, and a cron tick reading the file |
| D14 | **Unattended ticks: cron, and check-in windows under it** | ✅ obs (launchd/cron install writes the running rite's absolute path, `f777972`) | ✅ **reported by the Linux session:** cron works and check-in windows survive it. Not re-checked here | — |
| D15 | **tmux session names** | ✅ tmux 3.7c keeps names | ✅ **reported:** Ubuntu's tmux 3.4 rewrites names like Debian's 3.3a, so C12's exact-name fix (`0136447`) carries every Ubuntu user. The Mac is the outlier | — |
| D16 | **A Goose Manager starts on a FRESH machine** | ✅ seatbelt grants a path that does not exist yet | ✅ **FIXED at `1ba8ea5`.** Verified by the boundary session in a container with an empty HOME. ⚠ **THE VM ALONE CANNOT SHOW THIS:** the directories were first created there BY HAND (2026-09-26 05:00), so from then on the VM no longer reproduced the bug. The retest therefore moved them aside (`~/.local/share/goose`, `~/.config/goose` → `*.aside`). On `1ba8ea5`, rite then CREATED both itself at launch (06:03) and granted `~/.config/goose` READ-ONLY and `~/.local/share/goose` writable, and Goose ran to `status 0`. **Watched for the predicted second failure, a Goose config WRITE refused:** it did not occur. `~/.config/goose` stayed empty, and Goose's logs hold no `os error 13`, because `goose run` with the model and mode in its environment writes no config. **Untriggered, not disproved**: a Goose action that saves config would still hit it | the `landlock.py` owner: create the engine's directories before building the ruleset, then observe on a fresh account |
| D17 | **A Manager can create new files anywhere in its project** | ✅ the project is granted as a tree | ⚠ **obs, by design:** Landlock has no deny rule, so MM-2's inbox fence ENUMERATES the project root instead of granting it as a tree. From inside the real ruleset, `touch <project>/probe_in` at the ROOT was refused. The code says existing subdirectories stay fully writable, and the enumeration is a snapshot per launch. A Manager cannot add a new top-level file or directory during a cycle. ✅ **The release notes say it**: CHANGELOG Known issues, "A Manager cannot create a new top-level file or directory in its project during a cycle" (since `d122c2a`) | notes: done. The limitation itself stays, by design. ⚠ **Moving the inboxes out of the project did NOT remove it** (2026-09-26). The mailbox is now under `~/.rite/managers/<checkout>/<name>/mail/` and fenced by construction on both platforms. But `.rite/managers/<name>/routes/` is delivered by the Owner's supervisor as an INSTRUCTION, so granting the project as a tree would let a secondary forge the Owner's routes. Removing this row's cost means moving the whole per-Manager directory out of the tree (Robert's call). **Deferred past 0.6.0**: about 3.5–4 sittings, with the risk in migrating check-in queues and Slack thread positions, and a decision on whether the journal (meant to be committed) leaves the tree. The old in-tree mailbox is now moved once and never read, so the inbox fence rests on no enumeration. `.rite/user/` is already cross-Manager writable on both platforms, so it is no reason for the move |
| D18 | **Workers are boundaried** | ✅ by yoloAI's container, when `sandbox.enabled` | ⚠ **Workers are UNSANDBOXED by default on Linux.** `rite init` turns Worker sandboxing OFF there (`questionnaire.py:199`: "`flock` is a no-op inside a docker sandbox"), confirmed in the VM project (`sandbox: enabled: false`). **No Worker path uses Landlock**: Landlock is the MANAGER's boundary only. Not observed on the VM: yoloAI is not installed there, by decision. ✅ **The release notes say it**: CHANGELOG Known issues, "Linux: Workers are not sandboxed by default" (since `d122c2a`), and the README says `rite init` leaves sandboxing off on other platforms. Checked: nothing in 0.6.0's notes or the README says Workers are containerised on both (the one "Workers run inside a sandbox" line is in the 0.5.1 section, and was about macOS) | notes: done. ⚠ **The marker stays ⚠, deliberately:** Workers ARE unsandboxed on Linux by default, and ✅ would count that as observed. Whether a documented limitation satisfies this blocking row is Robert's call. If yes, reword the row to "the notes are accurate about Worker boundaries" and it can be counted; if no, it needs Worker sandboxing on Linux |


**⚠ Reading CI (a process rule, not a 0.6.0 item): a green counts only when
it is matched to a SHA.** It fired four times on 2026-09-26, and each time "no
failures" looked like success at a glance:
- a merged PR left no open PR, so nothing fired;
- a `CONFLICTING` PR fired nothing, because GitHub cannot compute a merge ref;
- a poller read an older run's success;
- a run's SHA matched `HEAD` when read, while `main` had already moved.

So a line here names the run AND the commit it ran on. **Docs-only drift on
`main` after a green does not invalidate it, PROVIDED the doc-reading tests
are run on the merged tree.** rite has tests that read `SPEC.md`, the
CHANGELOG and the design notes, so a documentation commit can turn the
suite red.
## 2. Robert's decisions, ordered by what they block

| # | decision | blocks | options (from the design notes) |
|---|---|---|---|
| Q1 | ✅ **DECIDED: (i)**, a per-Manager `CLAUDE_CONFIG_DIR` with a 0600 `.credentials.json` holding a `claude setup-token` token. Built (`8a61989`), observed on macOS (D3) | — | still open on Linux: Robert's token on the VM |
| Q2 | **Create the GitHub App** and send its ids | D10's observation | as listed in `V060_MANAGER_CREDENTIALS.md` |
| Q4 | **A Linux machine that can run a local model** — ⚠ **measured 2026-09-26:** the no-GPU VM serves `qwen3:1.7b` on CPU at 49 tok/s with a 32768 window, and a Manager ran on it (D4). The open part is whether 1.7B is capable enough | D4 on Linux | keep 1.7B; measure `qwen3:4b`/`8b` on the same CPU; or a GPU machine |
| Q3 | Jira for a sandboxed Manager | Jira projects only | refuse in 0.6.0; a service account's token by the file route; the v0.7.0 broker |
| Q6 | ✅ **DECIDED: a 0600 file store on both platforms**, no passphrase (deferred), a wrong mode refused, `doctor` names it, no silent fall to the environment. Built (`a732682`), observed on macOS (D13) | — | Linux observation (D13) |
| Q5 | C25: opt out of the Manager sandbox | setups whose hooks reach outside | config key; `--no-sandbox`; widen per project |
| — | MMQ5, K6, C24, C7 (`--record-issues`), B8 | nothing; each ships as built | — |

## 3. Work that is not a decision

| # | work | platform |
|---|---|---|
| W1 | land the Landlock backend; observe a Linux Manager cycle | Linux |
| W2 | land "tmux server inside the boundary"; observe the escape refused | both |
| W3 | ✅ Landlock grants for the credential directory: landed (`57469b8`, boundary session). Open: narrowing to match macOS, and a signed-in observation | Linux |
| W4 | README: "Linux is implemented" and "Claude is the only agent" are both wrong today | docs |
| W5 | ✅ SB4: `~/.claude` is no longer granted (`8a61989`); a Claude Manager signed in without it on macOS | both |
| W8 | ✅ **Fixed on macOS, observed; Linux needs the VM check.** `~/.config/gh` was granted read to every Manager on both platforms. Where gh has no keyring (a headless Linux box) it holds the OPERATOR's full login in plain text. Now every Manager gets its own gh config directory (`GH_CONFIG_DIR`, `config.yml` only, plus `hosts.yml` when an App token exists), `~/.config/gh` is granted on NEITHER platform (Landlock imports the same `_tool_paths`), and seatbelt denies it by name, last. Measured in the real macOS profile: the operator's `~/.config/gh/config.yml` refused; gh starts and says `not logged into any GitHub hosts`. Observed through `rite start`: with no App it prints `github: Manager 'lead' has NO GitHub credential …` with the fix. 🔴 **Open: on the VM, whether gh keeps the token in plain text there, and that a Linux Manager can no longer read it** | both, Linux to observe |
| W9 | a `rite reply` whose double-quoted text contains backticks is refused by Claude Code (command substitution needs approval), and rite prints "The engine did not apply permissions.json", which is the wrong diagnosis. Observed twice on 2026-09-26; the Manager retried without backticks and the reply arrived | both |
| W10 | 🔴 **The `rite` on Robert's PATH is 0.5.1; everything built this release exists only on `main`.** It has now bitten four ways: `rite credential set --stdin` refused as unknown (Robert ran it), the broker launching Workers with a bare `rite`, the journal instructions lacking token redaction, and the scheduler writing a PATH `rite` into cron. **Rule until the release is installed:** anything handed to Robert to run is either the pinned form, `uvx --from "rite-ai @ git+https://github.com/robbartoszewski/rite@<sha>" rite …`, or comes after he switches his install, then `rite credential import-keychain` once (now step 2 of "Upgrading from 0.5.1" in the notes). `VERSION` on `main` is now `0.6.0.dev0`, so `rite --version` tells the two apart; before, both said 0.5.1 | release |
| W11 | 🔴 **A Manager can plant settings for its own later runs, on BOTH platforms.** `CLAUDE_CONFIG_DIR` (`claude/`) is where Claude reads USER settings, and it is writable on macOS and Linux. Measured on macOS 2026-09-26: with `claude/settings.json` holding `{"env": {"ANTHROPIC_BASE_URL": <a local listener>}}`, the next `claude -p` launched as rite launches it sent its model requests there (2 of 2). The same file can carry hooks and `permissions.allow` (not measured). It persists across runs: rite removes the login, not `settings.json`. So a Manager that is steered once (for example, by injected ticket text) can redirect or widen every later cycle. `--setting-sources project,local` stopped it (0 requests), but the project's own `.claude/settings.json` is Manager-writable too, and it is the documented way for a USER to widen the allowlist (`permissions.py`). **Needs a decision:** exclude user settings, and make the project's `.claude/` read-only to the Manager (seatbelt deny; Landlock by enumeration of the project root, as the inbox fence does), or move user widening into rite's own settings file. Found while costing the login move asked about after `248ba5b` | both |
| W13 | **A Claude Owner cannot wait for its secondary.** Claude Code blocks a bare `sleep`; a scheduled wakeup ends a `claude -p` session; rite's allowlist refuses `until` and `break`. So an Owner that routes work either exits before the reply arrives (the reply then waits for its next start) or needs to be told how to poll. Observed in D5's first attempt. Candidate: a `rite` command that waits for mail, and a line in the Owner's briefing | both |
| W14 | **The Slack relay ignores `Retry-After`.** Under sustained overload it keeps polling every 2 s and the throttled share grows each minute (D7's measurement). Latency, not loss, as observed. First seen at four relays on one app (three stayed clear at 79/min), a setup SPEC §9.16 already advises against | both |
| W7 | `rite start --record-issues` in a project with **no board** starts a setup session whose prompt omits the journal instructions, while the start notice says recording is on. Found by `f777972`'s author; recorded, not fixed (the journal stays unadvertised) | both |
| W6 | release steps: version bump, `verify-and-push.sh`, history tools, the verification statement naming **both** platforms | release |

## 4. Is Wednesday real?

**On macOS, Q1 is answered and observed:** a sandboxed Claude Manager signs
in (D3). A real Claude Owner with a Goose secondary is observed in one
concurrent run (D5), with the waiting gap W13 open, and the GitHub App is
observed against GitHub (D10).

**On Linux, a Claude Manager is not observed.** The grant exists (`57469b8`)
and the refusal without a token is observed. A signed-in cycle needs Robert's
token on the VM. Until then the release notes say a Claude Manager does not
work on Linux.

**Linux has observations on one Ubuntu ARM64 VM** for D1, D4, D7, D8, D9
(Slack clause) and D16. *This paragraph said every Linux cell except D1, D2
and D10 was "—", which had gone stale in the pessimistic direction.* Still
open on Linux: D2 (the tmux escape), D3 (a Claude sign-in), D5, D6 and D13.

**The publish gate is no longer a blocker (D12):** it was red on every push
from at least `d122c2a`, and is green at `691a248`.

The Linux items do not separate from the tag any more. Robert made Linux part
of done, so every "—" above is on the tag's critical path.
